# .claude/agents/

Custom Claude Code subagent definitions (Markdown with frontmatter). `system-implementer.md`
builds/extends gameplay systems in the ADR-0018 architecture (pairs with the `system-dev` skill).
