# Goal prompt: implement Open-world Wildlife

Handover from the design/grilling session to an implementation session. Paste the block below as one
command (pair with auto mode; review the diff like a PR when it finishes). Source of truth:
`.claude/plans/feature-plan-open-world-wildlife.md` and
`docs/adr/0012-open-world-wildlife-host-simulated-danger-gradient.md`.

The `/goal` built-in caps the condition at 4000 characters, so the block below is a trimmed spine
that leans on the feature plan + ADR-0012 for full detail (do not inline more — point the
implementer at those files). Paste it as one command:

```
/goal Implement .claude/plans/feature-plan-open-world-wildlife.md per ADR-0012: every "Resolved decisions" item is in code and every single-player-verifiable "Acceptance criteria" holds, honoring its "Scope boundaries — do NOT build" and "Constraints & gotchas". Build in the plan's order, committing each verified step: (1) pure node-importable content — hide/meat/trophy ResourceIds, peaceful+predatory MobProfiles (speed strictly ≤ player even vs the +20% fish buff; aggro/flee/skittish flags) and an open-world spawn planner reusing dungeon.ts stepMob/createMob/applyMobHit (reskin, no new brain), plus EN+DE strings and icons; (2) a per-Zone `dangerous` flag in tools/generate-map.ts + a zoneAt-style danger lookup, then npm run genmap keeping all Zone rects and Node ids byte-stable; (3) config — KNOCKDOWN_STUN_MS 5000→3000 globally + creature constants (density, night multiplier, aggro, speeds, rolling-window length); (4) the creature Host — deterministic election from presence (lowest-sorting online name, zero negotiation, reuse SupabaseBackend.onPresenceSync), an ephemeral in-memory pool spawned around online players, per-tick stepMob, and ONE batched `creatures` broadcast per tick on jw-world, spatially culled; (5) non-host rendering interpolating remote creature state (mirror the Delve host/guest split); (6) combat+forage — predator hits via applyMobHit, peaceful creatures foraged as moving Nodes, catch=knockdown+3s stun + rolling-window Exhaustion (wake Hammock/spawn, inventory intact) distinct from the Guardian counter, predators de-aggro and never cross the safe-core threshold; (7) rewards into EXISTING loops only — Village pool entries, a cooked-meat recipe granting the EXISTING move buff (new ingredient, not a new buff), trophy/decor Structures; (8) a ?wild dev flag. Prove completion with transcript evidence, never bare claims: show npm run build exits 0; drive the dev app with preview tools for every solo-verifiable AC (MockBackend's lone player IS host — ?wild to land in a danger Zone, ?pump&canvas hidden tab via __jw/__game), naming the AC each demo proves (AC1-9, AC12, AC13); EXPLICITLY flag AC10 (one batched broadcast under the realtime cap, 8 players) and AC11 (single-host election + graceful re-elect/respawn on host-leave) as implemented-to-spec but REQUIRING MANUAL MULTIPLAYER PLAYTEST, never proven; end with a self-audit confirming NONE of the "do NOT build" items shipped (no player HP, no item loss, no new buff, no armour/stat-weapon/tool tier, no Village-mechanic change, no core predators, no host-migration protocol, no new AI engine, no server/tick loop, no Husks-in-world/Wildlife-in-Dungeon, no map id shift, no new Dungeon/boss, no Guardian reactivity) and a clean git tree. Constraints: keep dungeon.ts + the new creature module free of browser globals and ../config (tile units, tiles/second); never add per-creature presence or unbatched broadcasts (jw-world is near the msg/s cap; presence track() >~0.5/s trips the limit); use CONTEXT terms (Wildlife never monster/enemy/mob; forage/hunt; knockdown/Exhaustion never death; safe core vs danger wilds); NO migration, RPC, or edge function; if the shorter stun softens the Guardian note it for separate tuning, don't revert; npm/npx need --registry https://registry.npmjs.org/; commit each step on branch open-world-wildlife (create if needed). If blocked or after 80 turns, stop and report which ACs remain unmet and why.
```
