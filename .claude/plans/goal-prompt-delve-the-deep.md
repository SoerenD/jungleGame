# Goal prompt: implement the Deep (the Delve's second Stage)

Handover from the design/grilling session to an implementation session. Paste the block below as one
command (pair with auto mode; review the diff like a PR when it finishes). Source of truth:
`.claude/plans/feature-plan-delve-the-deep.md` and `docs/adr/0011-chained-multi-stage-dungeons.md`.

```
/goal The feature plan .claude/plans/feature-plan-delve-the-deep.md is fully implemented: every decision in its "Resolved decisions" section is in code and every Given/When/Then in its "Acceptance criteria" section that is verifiable single-player demonstrably holds, while respecting its "Scope boundaries — do NOT build" and "Constraints & gotchas" sections and ADR-0011. Work in this build order, verifying and committing each step before starting the next: (1) new content data — ResourceIds cinder_shard + forge_core, ToolId forgebrand with its ADR-0006 weapon-table entry (slower attack speed + larger per-hit band tuned so net DPS ≈ the Sword) in src/content/guardian.ts, the Forgebrand recipe (forge_core + cinder_shard + planks + stone, no Sword consumed), plus EN+DE strings and icons; (2) the Deep interior and mobs as pure, node-importable data in src/content/dungeon.ts and src/delveProps.ts — the fixed magma layout (safe entry → 2–3 Husk rooms → a boss room large enough for safe eruption edges), Cinder Husk + Ember Husk + Forgeborn MobProfiles reusing the existing state machine (reskin/retune, slightly harder), per-descender scaling, and new molten PropKinds; (3) mob sprites (Cinder Husk, Ember Husk, Forgeborn) in src/mobSprites.ts; (4) boss-1 rework — split completeDelveRun so the Deep Guardian's death pays Stage-1 participation loot, shakes the screen, and opens a lingering hidden door instead of exiting the instance; (5) descent — pressing interact at the open door starts the Deep as a FRESH run (new runId, host = the presser, roster = the non-Exhausted players at the door, which can only shrink), by extending DungeonMsg 'start' with a Stage/layout marker, relaxing the guest inDelve join-guard so at-the-door party-mates follow, and reusing teardown/build parameterized by Stage; (6) the Forgeborn eruption — an oversized, long-telegraphed, radius-based strike via the existing windup→strike + telegraph renderer (not a new engine, not line-of-sight); (7) Deep completion — the Forgeborn's death pays forge_core + cinder_shard to Deep participants and exits everyone to the World, plus the Deep's own exit tile and wipe/host-leave handling; (8) the ?deep dev flag. Prove completion with evidence in the transcript, never bare assertions: (a) npm run build exits 0 — show the output; (b) drive the running dev app with the preview tools to demonstrate each single-player-verifiable criterion — use ?deep to land directly in the Deep and ?dungeon/?fight as needed, and load a hidden tab with ?pump&canvas driving __jw/__game — stating which acceptance criterion each demonstration proves; in single-player MockBackend the lone player IS the host, so door-open + linger (AC1), press-interact descent into the magma interior (AC2), scaling (AC4), Cinder/Ember Husks (AC5), the Forgeborn + eruption (AC6), Deep loot + completion (AC7), the Forgebrand craft/stats/use (AC8), Deep wipe keeping Stage-1 loot (AC9), the per-run door (AC10), and no open-world/backend change (AC11) are all demonstrable solo; (c) EXPLICITLY flag the multiplayer-only behaviors — roster-shrink across real clients (AC3), host = descent initiator with guests auto-following, and the relaxed join-guard — as implemented-to-spec but REQUIRES MANUAL MULTIPLAYER PLAYTEST; never claim them proven; (d) a closing self-audit lists every item in "Scope boundaries — do NOT build" and confirms none was built, with git status showing a clean tree. Constraints while working: use CONTEXT.md terminology everywhere (Stage never level; the Deep; the Forgeborn; Cinder/Ember Husk; Husk never enemy/monster; knockdown/Exhaustion never death); keep src/content/dungeon.ts, src/delveProps.ts and src/content/guardian.ts free of browser globals and ../config imports; introduce NO database migration and NO new RPC — reuse jw_claim_delve_loot exactly; do not touch Stage-1 content, its loot, or the Sword; call npm/npx with --registry https://registry.npmjs.org/; commit each verified step on a dedicated feature branch (create one, e.g. delve-the-deep, if not already on one for this feature). If genuinely blocked, or after 80 turns, stop and report exactly which acceptance criteria remain unmet and why.
```
