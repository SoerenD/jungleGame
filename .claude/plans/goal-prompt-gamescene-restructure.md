# Goal prompt: execute the GameScene restructure (ADR-0018)

Handover from the 2026-07-17 grill session to an implementation session. Paste the block below
as one command (pair with auto mode; review the result like a PR when it finishes). Source of
truth: `.claude/plans/feature-plan-gamescene-restructure.md` and
`docs/adr/0018-humble-scene-system-decomposition.md` — the condition below is a trimmed spine
that leans on those files for full detail (system table with line ranges, GameContext spec,
update() ordering, acceptance criteria). Do not inline more; point the implementer at them.

The `/goal` built-in caps the condition at 4000 characters. Paste as one command:

```
/goal Execute .claude/plans/feature-plan-gamescene-restructure.md per docs/adr/0018-humble-scene-system-decomposition.md: the 8,230-line src/scenes/GameScene.ts is restructured into a humble Scene + 18 plain-TS systems in src/systems/ with ZERO player-observable behavior change, every plan-§5 acceptance criterion holds, §7 "explicitly NOT in this change" is honored, §8 gotchas respected. Work as stacked commits on master, in the plan's §4 order, npm run build green before EVERY commit: (0) commit the pending working-tree drop-item batch verbatim as its own commit; (1) scaffolding — Mode ('overworld'|'delve'), GameSystem interface (create/update(time,dt)/destroy), GameContext (scene, backend, bus, world, me, player, mode, held state, readonly inventory + setInventory() as the ONLY mutate+emit path), typed bus (GameEvents name→payload map as generics on the existing ui/bus.ts singleton, no new dependency), empty ordered dispatch, behavior unchanged; (2–18) extract ONE system per commit, easiest→hardest: sceneFx helpers (death/node FX), Fog (fog+vista+checkZone+labels), Atmosphere (day/night, veils, fireflies, elevation/waterfall, audio volumes), Fishing, Seal, Village, Progression (journey/hints/treasure/lore), District (realm gates/camera), Stations (craft/eat/drop/crate/sawmill/refiner glue), Harvest, Build (placement/ghost/dismantle), Presence, Echo, Wildlife, Delve, Fight+Projectile (folding the four mire*/echo*/verdant*/reverb* field blocks into Record<WardenId,BossRig> on the activeBoss() seam), Input+Player LAST; (19) GameScene ≤ ~700 lines, __jw/__game dev handles re-pointed with stable names, dead-code sweep; (20) AFTER the code ships, author .claude/skills/system-dev/ (SKILL.md + TEMPLATE-{system,rule-module,bus-event,wiring}.md + CHECKLIST.md — every template COPIED from real shipped code) and .claude/agents/system-implementer.md (loads that skill first), then prove the loop: the agent dry-runs a DummySystem scaffold to build-green and the dummy is reverted. Hard rules: preserve the EXACT update() ordering (plan §8), documented as a numbered list in GameScene; preserve inDelve early-return semantics (in delve mode NO overworld system ticks); resolveEAction stays ONE ordered priority chain in InputSystem; per-frame cross-system reads use explicit refs wired by GameScene, HUD stays bus-only; ui/hud.ts is NOT restructured and its event names/payloads are a frozen contract; Backend interface + both implementations untouched beyond step 0; no new dependencies, no DB migration, no genmap change, no balance change; every system detaches its bus listeners in destroy(); content/ extractions stay node-importable (no browser globals, no ../config). Prove completion with transcript evidence, never bare claims: npm run build exit 0 per commit; then a FULL mechanic browser smoke with zero console errors via preview tools (MockBackend by blanking VITE_SUPABASE_* in .env.development.local; hidden tab needs ?pump&canvas + __jw/__game): harvest with axe double-damage + regrow (?slowregrow), craft+place+dismantle, sawmill+crate round-trip, ?fight full Guardian loop (Eye-Window-only damage, melee+bow, participation Scales), a Delve run (enter→fell→descend→exit→loot), wildlife spawn + arrow enrage + calm (never into the Village), fishing bite→reel + cook + speed buff, seal + village contribute bars, ?night darkness + Hand-Torch glow + fireflies, chat + signpost, presence/remote render; then run an adversarial multi-agent review of the full diff and fix confirmed findings, re-smoking what they touched; only when ALL of the above hold, push once and confirm the Pages deploy is green. If blocked, or after 120 turns, stop and report which steps/ACs remain unmet and why.
```
