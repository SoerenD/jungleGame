# .claude/plans/

Long-form implementation plans and session handovers (one Markdown file per feature/ADR).
This is where big prompts and multi-step plans live so `CLAUDE.md` at the root can stay lean.

- Historical scratch space — plans reflect intent at authoring time and may be stale; trust the
  code and ADRs over an old plan.
