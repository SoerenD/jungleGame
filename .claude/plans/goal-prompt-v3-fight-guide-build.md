# Goal prompt: implement the v3 feature plan

Paste this as one command (pair with auto mode; review the diff like a PR when it finishes):

```
/goal The feature plan .claude/plans/feature-plan-v3-fight-guide-build.md is fully implemented: every decision in its "Resolved decisions" section is in code and every Given/When/Then in its "Acceptance criteria" section demonstrably holds, while respecting its "Scope boundaries — do NOT build" and "Constraints and gotchas" sections. Work in the plan's build order (hold-E → Journey onboarding → Avatar customizer → Guardian rework → functional Structures + plank economy); verify each feature before starting the next, and commit it (run git init for the first commit if this folder is not yet a repository). Prove completion with evidence in the transcript, never bare assertions: (1) `npm run build` exits 0 — show the output; (2) each acceptance-criteria group (Guardian, Hold-E, Journey, Avatars, Structures) is demonstrated against the running dev app using the preview tools (load with ?pump&canvas when the tab is hidden; use ?fight for Guardian criteria) or, for pure logic such as the deterministic fight schedule, via small tsx scripts — stating which criterion each demonstration proves; (3) a closing self-audit lists every scope boundary from the plan and confirms nothing out-of-scope was built, with `git status` output showing a clean tree. Constraints while working: use CONTEXT.md terminology in all code, UI text, and docs; keep src/content/guardian.ts free of browser globals and ../config imports; all fight behavior (waves, fury phases, Eye Windows, lunges) must derive from summonedAt + elapsed time only, never from HP; call npm/npx with --registry https://registry.npmjs.org/. If genuinely blocked, or after 80 turns, stop and report exactly which acceptance criteria remain unmet and why.
```
