# public/assets/characters/

Character spritesheets. Holds `character.png` (the original ArMM1998 4-dir walk cycle).

- Legacy: no longer loaded since v3 — Player Avatars are composed at runtime from four palette
  picks (`src/avatars.ts`). Kept for reference/credits only.
